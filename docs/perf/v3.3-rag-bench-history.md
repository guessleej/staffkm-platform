# RAG bench history

每週日 02:00 UTC 由 `.github/workflows/rag-eval.yml` 自動跑、append 結果。

格式：日期 + bench result table（mode+rerank / hit@5 / MRR）。

退化 detection：若 `hybrid+none` 的 hit@5 < 0.5，CI fail。

手動 workflow_dispatch 也會 append 並 commit。

---
