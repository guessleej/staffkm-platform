apiVersion: 1
datasources:
  - name: staffkm-tempo
    type: tempo
    uid: staffkm-tempo
    url: http://tempo:3200
    editable: false
    jsonData:
      tracesToLogsV2:
        datasourceUid: 'staffkm-loki'
        tags: ['service']
        spanStartTimeShift: '-5m'
        spanEndTimeShift: '5m'
      serviceMap:
        datasourceUid: 'staffkm-prometheus'   # v3.3 B3: 對齊 prometheus.yml uid
      tracesToMetrics:
        datasourceUid: 'staffkm-prometheus'
        spanStartTimeShift: '-2m'
        spanEndTimeShift: '2m'
